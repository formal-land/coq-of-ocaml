Exception ('TailLess', ())

Exception ('Wtf', (Type ('Z') * Type ('string')))

(. -[ 'TailLess' ]-> .,
  Value (non_rec, 'f', [ 'A'; 'B' ], [ ('x', 'B') ], Some 'A') =
    Apply
      ((7, Effect ([ 'TailLess' ], .)),
        Variable ((7, Effect ([ ], . -[ 'TailLess' ]-> .)), 'raise_TailLess'),
        Tuple ((?, Effect ([ ], .)))))

Module 'G':
  (. -[ 'Wtf' ]-> .,
    Value (non_rec, 'g', [ 'A' ], [ ('x', Type ('Z')) ], Some 'A') =
      Apply
        ((10, Effect ([ 'Wtf' ], .)),
          Variable ((10, Effect ([ ], . -[ 'Wtf' ]-> .)), 'raise_Wtf'),
          Tuple
            ((?, Effect ([ ], .)), Variable ((10, Effect ([ ], .)), 'x'),
              Constant ((10, Effect ([ ], .)), String("no")))))

(. -> . -[ 'IO'; 'NonTermination'; 'TailLess' ]-> .,
  Value
    (rec, 'h_rec', [ 'A' ],
      [ ('counter', Type ('nat')); ('l', Type ('list', 'A')) ], Some 'A') =
    Match
      ((?, Effect ([ 'IO'; 'NonTermination'; 'TailLess' ], .)),
        Variable ((?, Effect ([ ], .)), 'counter'),
        [
          (Nat(0),
            Apply
              ((?, Effect ([ 'NonTermination' ], .)),
                Variable
                  ((?,
                    Effect
                      ([ ],
                        .
                          -[
                            'NonTermination'
                          ]-> .)),
                    'not_terminated'),
                Variable ((?, Effect ([ ], .)), 'tt')));
          (Constructor ('S', 'counter'),
            Match
              ((14,
                Effect
                  ([ 'IO'; 'NonTermination'; 'TailLess' ],
                    .)),
                Variable ((14, Effect ([ ], .)), 'l'),
                [
                  (Constructor ('[]'),
                    Sequence
                      ((15,
                        Effect
                          ([
                            'IO';
                            'TailLess'
                          ],
                            .)),
                        Apply
                          ((15,
                            Effect
                              ([
                                'IO'
                              ],
                                .)),
                            Variable
                              ((15,
                                Effect
                                  ([
                                  ],
                                    .
                                      -[
                                        'IO'
                                      ]->
                                      .)),
                                'print_string'),
                            Constant
                              ((15,
                                Effect
                                  ([
                                  ],
                                    .)),
                                String("no tail"))),
                        Apply
                          ((15,
                            Effect
                              ([
                                'TailLess'
                              ],
                                .)),
                            Variable
                              ((15,
                                Effect
                                  ([
                                  ],
                                    .
                                      -[
                                        'TailLess'
                                      ]->
                                      .)),
                                'raise_TailLess'),
                            Tuple
                              ((?,
                                Effect
                                  ([
                                  ],
                                    .))))));
                  (Constructor
                    ('cons',
                      'x',
                      Constructor
                        ('[]')),
                    Variable
                      ((16,
                        Effect
                          ([
                          ],
                            .)),
                        'x'));
                  (Constructor ('cons', Any, 'xs'),
                    Apply
                      ((17,
                        Effect
                          ([
                            'IO';
                            'NonTermination';
                            'TailLess'
                          ],
                            .)),
                        Apply
                          ((?,
                            Effect
                              ([
                              ],
                                .
                                  -[
                                    'IO';
                                    'NonTermination';
                                    'TailLess'
                                  ]->
                                  .)),
                            Variable
                              ((?,
                                Effect
                                  ([
                                  ],
                                    .
                                      ->
                                      .
                                        -[
                                          'IO';
                                          'NonTermination';
                                          'TailLess'
                                        ]->
                                        .)),
                                'h_rec'),
                            Variable
                              ((?,
                                Effect
                                  ([
                                  ],
                                    .)),
                                'counter')),
                        Variable
                          ((17,
                            Effect
                              ([
                              ],
                                .)),
                            'xs')))
                ]))
        ]))

(. -[ 'Counter'; 'IO'; 'NonTermination'; 'TailLess' ]-> .,
  Value (non_rec, 'h', [ 'A' ], [ ('l', Type ('list', 'A')) ], Some 'A') =
    Let (?, Effect ([ 'Counter'; 'IO'; 'NonTermination'; 'TailLess' ], .))
      (non_rec, 'counter', [ ], [ ], None) =
      Apply
        ((?, Effect ([ 'Counter' ], .)),
          Variable ((?, Effect ([ ], . -[ 'Counter' ]-> .)), 'read_counter'),
          Variable ((?, Effect ([ ], .)), 'tt')) in
    Apply
      ((?, Effect ([ 'IO'; 'NonTermination'; 'TailLess' ], .)),
        Apply
          ((?, Effect ([ ], . -[ 'IO'; 'NonTermination'; 'TailLess' ]-> .)),
            Variable
              ((?,
                Effect ([ ], . -> . -[ 'IO'; 'NonTermination'; 'TailLess' ]-> .)),
                'h_rec'), Variable ((?, Effect ([ ], .)), 'counter')),
        Variable ((?, Effect ([ ], .)), 'l')))
