Exception ('TailLess', ())

Exception ('Wtf', (Type ('Z') * Type ('string')))

(. -[ 'TailLess' ]-> .,
  Value (non_rec, 'f', [ 'A'; 'B' ], [ ('x', 'B') ], Some 'A') =
    Apply
      ((unknown, Effect ([ 'TailLess' ], .)),
        Variable ((unknown, Effect ([ ], . -[ 'TailLess' ]-> .)), 'TailLess'),
        Tuple ((unknown, Effect ([ ], .)))))

(. -[ 'Wtf' ]-> .,
  Value (non_rec, 'g', [ 'A'; 'B' ], [ ('x', 'B') ], Some 'A') =
    Apply
      ((unknown, Effect ([ 'Wtf' ], .)),
        Variable ((unknown, Effect ([ ], . -[ 'Wtf' ]-> .)), 'Wtf'),
        Tuple
          ((unknown, Effect ([ ], .)),
            Constant ((unknown, Effect ([ ], .)), Int(12)),
            Constant ((unknown, Effect ([ ], .)), String("no")))))

(. -> . -[ 'IO'; 'NonTermination'; 'TailLess' ]-> .,
  Value
    (rec, 'h_rec', [ 'A' ],
      [ ('counter', Type ('nat')); ('l', Type ('list', 'A')) ], Some 'A') =
    Match
      ((unknown, Effect ([ 'IO'; 'NonTermination'; 'TailLess' ], .)),
        Variable ((unknown, Effect ([ ], .)), 'counter'),
        [
          (Nat(0),
            Apply
              ((unknown, Effect ([ 'NonTermination' ], .)),
                Variable
                  ((unknown,
                    Effect
                      ([ ],
                        .
                          -[
                            'NonTermination'
                          ]-> .)),
                    'not_terminated'),
                Variable ((unknown, Effect ([ ], .)), 'tt')));
          (Constructor ('S', 'counter'),
            Match
              ((unknown,
                Effect
                  ([ 'IO'; 'NonTermination'; 'TailLess' ],
                    .)),
                Variable ((unknown, Effect ([ ], .)), 'l'),
                [
                  (Constructor ('[]'),
                    Sequence
                      ((unknown,
                        Effect
                          ([
                            'IO';
                            'TailLess'
                          ],
                            .)),
                        Apply
                          ((unknown,
                            Effect
                              ([
                                'IO'
                              ],
                                .)),
                            Variable
                              ((unknown,
                                Effect
                                  ([
                                  ],
                                    .
                                      -[
                                        'IO'
                                      ]->
                                      .)),
                                'print_string'),
                            Constant
                              ((unknown,
                                Effect
                                  ([
                                  ],
                                    .)),
                                String("no tail"))),
                        Apply
                          ((unknown,
                            Effect
                              ([
                                'TailLess'
                              ],
                                .)),
                            Variable
                              ((unknown,
                                Effect
                                  ([
                                  ],
                                    .
                                      -[
                                        'TailLess'
                                      ]->
                                      .)),
                                'TailLess'),
                            Tuple
                              ((unknown,
                                Effect
                                  ([
                                  ],
                                    .))))));
                  (Constructor
                    ('cons',
                      'x',
                      Constructor
                        ('[]')),
                    Variable
                      ((unknown,
                        Effect
                          ([
                          ],
                            .)),
                        'x'));
                  (Constructor ('cons', Any, 'xs'),
                    Apply
                      ((unknown,
                        Effect
                          ([
                            'IO';
                            'NonTermination';
                            'TailLess'
                          ],
                            .)),
                        Apply
                          ((unknown,
                            Effect
                              ([
                              ],
                                .
                                  -[
                                    'IO';
                                    'NonTermination';
                                    'TailLess'
                                  ]->
                                  .)),
                            Variable
                              ((unknown,
                                Effect
                                  ([
                                  ],
                                    .
                                      ->
                                      .
                                        -[
                                          'IO';
                                          'NonTermination';
                                          'TailLess'
                                        ]->
                                        .)),
                                'h_rec'),
                            Variable
                              ((unknown,
                                Effect
                                  ([
                                  ],
                                    .)),
                                'counter')),
                        Variable
                          ((unknown,
                            Effect
                              ([
                              ],
                                .)),
                            'xs')))
                ]))
        ]))

(. -[ 'Counter'; 'IO'; 'NonTermination'; 'TailLess' ]-> .,
  Value (non_rec, 'h', [ 'A' ], [ ('l', Type ('list', 'A')) ], Some 'A') =
    Let (unknown, Effect ([ 'Counter'; 'IO'; 'NonTermination'; 'TailLess' ], .))
      (non_rec, 'counter', [ ], [ ], None) =
      Apply
        ((unknown, Effect ([ 'Counter' ], .)),
          Variable
            ((unknown, Effect ([ ], . -[ 'Counter' ]-> .)), 'read_counter'),
          Variable ((unknown, Effect ([ ], .)), 'tt')) in
    Apply
      ((unknown, Effect ([ 'IO'; 'NonTermination'; 'TailLess' ], .)),
        Apply
          ((unknown, Effect ([ ], . -[ 'IO'; 'NonTermination'; 'TailLess' ]-> .)),
            Variable
              ((unknown,
                Effect ([ ], . -> . -[ 'IO'; 'NonTermination'; 'TailLess' ]-> .)),
                'h_rec'), Variable ((unknown, Effect ([ ], .)), 'counter')),
        Variable ((unknown, Effect ([ ], .)), 'l')))
