Value
  (non_rec, 'f', [ ], [ ('n', Type ('Z')); ('b', Type ('bool')) ], Some
    Type ('Z')) =
  IfThenElse
    ('b', Apply (Apply ('Z.add', 'n'), Int(1)),
      Apply (Apply ('Z.sub', 'n'), Int(1)))
