(.,
  Value (non_rec, 'u', [ ], [ ], Some Type ('unit')) =
    Constructor ((?, Effect ([ ], .)), 'tt'))

(.,
  Value (non_rec, 'l1', [ 'A' ], [ ], Some Type ('list', 'A')) =
    Constructor ((?, Effect ([ ], .)), '[]'))

(.,
  Value (non_rec, 'l2', [ ], [ ], Some Type ('list', Type ('Z'))) =
    Constructor
      ((?, Effect ([ ], .)), 'cons', Constant ((?, Effect ([ ], .)), Int(1)),
        Constructor ((?, Effect ([ ], .)), '[]')))

(.,
  Value (non_rec, 'l3', [ ], [ ], Some Type ('list', Type ('Z'))) =
    Constructor
      ((?, Effect ([ ], .)), 'cons', Constant ((?, Effect ([ ], .)), Int(1)),
        Constructor
          ((?, Effect ([ ], .)), 'cons',
            Constant ((?, Effect ([ ], .)), Int(5)),
            Constructor
              ((?, Effect ([ ], .)), 'cons',
                Constant ((?, Effect ([ ], .)), Int(7)),
                Constructor
                  ((?, Effect ([ ], .)), 'cons',
                    Constant ((?, Effect ([ ], .)), Int(32)),
                    Constructor
                      ((?, Effect ([ ], .)), 'cons',
                        Constant ((?, Effect ([ ], .)), Int(15)),
                        Constructor ((?, Effect ([ ], .)), '[]')))))))

(.,
  Value (non_rec, 'o1', [ 'A' ], [ ], Some Type ('option', 'A')) =
    Constructor ((?, Effect ([ ], .)), 'None'))

(.,
  Value (non_rec, 'o2', [ ], [ ], Some Type ('option', Type ('Z'))) =
    Constructor
      ((?, Effect ([ ], .)), 'Some', Constant ((?, Effect ([ ], .)), Int(12))))

(.,
  Value (non_rec, 'c', [ ], [ ], Some Type ('ascii')) =
    Constant ((?, Effect ([ ], .)), Char("g")))

(.,
  Value (non_rec, 's', [ ], [ ], Some Type ('string')) =
    Constant ((?, Effect ([ ], .)), String("bla")))
