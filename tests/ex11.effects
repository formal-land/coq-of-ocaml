Module 'SizedString':
  Record 't':
    ([ ('name', Type ('string')); ('size', Type ('Z')) ])

(.,
  Value (non_rec, 'r', [ ], [ ], Some Type ('SizedString.t')) =
    Record
      ((9, Effect ([ ], .)),
        ('SizedString.name', Constant ((9, Effect ([ ], .)), String("gre"))),
        ('SizedString.size', Constant ((9, Effect ([ ], .)), Int(3)))))

(.,
  Value (non_rec, 'r'', [ ], [ ], Some Type ('SizedString.t')) =
    Record
      ((10, Effect ([ ], .)),
        ('SizedString.name', Constant ((10, Effect ([ ], .)), String("haha"))),
        ('SizedString.size', Constant ((10, Effect ([ ], .)), Int(4)))))

(.,
  Value (non_rec, 's', [ ], [ ], Some Type ('Z')) =
    Apply
      ((?, Effect ([ ], .)),
        Apply
          ((12, Effect ([ ], .)), Variable ((12, Effect ([ ], .)), 'Z.add'),
            Field
              ((12, Effect ([ ], .)), Variable ((12, Effect ([ ], .)), 'r'),
                'SizedString.size')),
        Field
          ((12, Effect ([ ], .)), Variable ((12, Effect ([ ], .)), 'r''),
            'SizedString.size')))

(. -> .,
  Value
    (non_rec, 'f', [ ], [ ('match_var_0', Type ('SizedString.t')) ],
      Some Type ('bool')) =
    Match
      ((?, Effect ([ ], .)), Variable ((?, Effect ([ ], .)), 'match_var_0'),
        [
          (Record (('SizedString.size', Int(3))),
            Constructor ((15, Effect ([ ], .)), 'true'));
          (Any, Constructor ((16, Effect ([ ], .)), 'false'))
        ]))

(.,
  Value (non_rec, 'b', [ ], [ ], Some Type ('bool')) =
    Apply
      ((18, Effect ([ ], .)), Variable ((18, Effect ([ ], . -> .)), 'f'),
        Variable ((18, Effect ([ ], .)), 'r')))

(.,
  Value (non_rec, 'b'', [ ], [ ], Some Type ('bool')) =
    Apply
      ((19, Effect ([ ], .)), Variable ((19, Effect ([ ], . -> .)), 'f'),
        Variable ((19, Effect ([ ], .)), 'r'')))

Module 'Point':
  Record 't':
    ([ ('x', Type ('Z')); ('y', Type ('Z')); ('z', Type ('Z')) ])
  
  (.,
    Value (non_rec, 'p', [ ], [ ], Some Type ('t')) =
      Record
        ((27, Effect ([ ], .)), ('x', Constant ((27, Effect ([ ], .)), Int(5))),
          ('y', Constant ((27, Effect ([ ], .)), Int(-3))),
          ('z', Constant ((27, Effect ([ ], .)), Int(1)))))
  
  (.,
    Value (non_rec, 'b', [ ], [ ], Some Type ('bool')) =
      Match
        ((29, Effect ([ ], .)), Variable ((29, Effect ([ ], .)), 'p'),
          [
            (Record (('x', Int(5)), ('z', Int(1))),
              Constructor ((30, Effect ([ ], .)), 'true'));
            (Any, Constructor ((31, Effect ([ ], .)), 'false'))
          ]))
