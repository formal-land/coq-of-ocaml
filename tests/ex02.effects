(.,
  Value (non_rec, 't0', [ ], [ ], Some Type ('unit')) =
    Constructor ((?, Effect ([ ], .)), 'tt'))

(.,
  Value (non_rec, 't1', [ ], [ ], Some (Type ('ascii') * Type ('string'))) =
    Tuple
      ((?, Effect ([ ], .)), Constant ((?, Effect ([ ], .)), Char("c")),
        Constant ((?, Effect ([ ], .)), String("one"))))

(.,
  Value
    (non_rec, 't2', [ ], [ ],
      Some
        (Type ('Z') * Type ('Z') * Type ('Z') * Type ('bool') * Type ('bool')))
    =
    Tuple
      ((?, Effect ([ ], .)), Constant ((?, Effect ([ ], .)), Int(1)),
        Constant ((?, Effect ([ ], .)), Int(2)),
        Constant ((?, Effect ([ ], .)), Int(3)),
        Constructor ((?, Effect ([ ], .)), 'false'),
        Constructor ((?, Effect ([ ], .)), 'true')))

(. -> .,
  Value (non_rec, 'f', [ 'A' ], [ ('x', 'A') ], Some ('A' * 'A')) =
    Tuple
      ((?, Effect ([ ], .)), Variable ((?, Effect ([ ], .)), 'x'),
        Variable ((?, Effect ([ ], .)), 'x')))

(.,
  Value (non_rec, 't3', [ ], [ ], Some (Type ('Z') * Type ('Z'))) =
    Apply
      ((?, Effect ([ ], .)), Variable ((?, Effect ([ ], . -> .)), 'f'),
        Constant ((?, Effect ([ ], .)), Int(12))))
