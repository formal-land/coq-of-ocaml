The Coq files in this folder are generated by coq-of-ocaml from the Tezos protocol. Many files may not compile, the complete list is available in the `_CoqProject` file (commented file names are not compiled). To build the project:
```sh
coq_makefile -f _CoqProject -o Makefile
make
```
